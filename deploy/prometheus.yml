global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # 后端当前未暴露 /metrics（接入 prometheus-client 后启用）
  # - job_name: 'backend'
  #   metrics_path: /metrics
  #   static_configs:
  #     - targets: ['backend:8000']

  - job_name: 'elasticsearch'
    static_configs:
      - targets: ['elasticsearch:9200']
